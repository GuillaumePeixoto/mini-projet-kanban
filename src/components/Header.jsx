import Logo from '../assets/images/logo.png'

function Header() {

    return(
        <header>
            <nav id='nav-bar'>
                <img src={Logo} id="logo-site" />
                <ul>
                    
                </ul>
            </nav>
        </header>
    );
}

export default Header;